name: Dummy CI/CD

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v3

    - name: Build step
      run: echo "🔨 Building dummy app..."

    - name: Test step
      run: echo "🧪 Running dummy tests..."

    - name: Deploy step
      run: echo "🚀 Deploying to fake environment..."
